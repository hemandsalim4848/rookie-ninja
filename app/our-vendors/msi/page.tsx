import MSIPage from '@/src/components/vendors/MSI';

export const metadata = {
  title: 'MSI | Rookie Ninja',
  description: 'Authorized MSI distributor — graphic cards, motherboards, and peripherals across Middle East & Africa.',
};

export default function MSIVendor() {
  return <MSIPage />;
}
