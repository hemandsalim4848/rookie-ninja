import DeliPage from '@/src/components/vendors/Deli';

export const metadata = {
  title: 'Deli | Rookie Ninja',
  description: 'Authorized Deli distributor — monochrome laser printers for office and workgroup environments across Middle East & Africa.',
};

export default function DeliVendor() {
  return <DeliPage />;
}
